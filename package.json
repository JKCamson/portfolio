{
  "name": "portfolio",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "dev:full": "vercel dev",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview"
  },
  "dependencies": {
    "resend": "^4.0.1",
    "zod": "^3.23.8"
  }
}
